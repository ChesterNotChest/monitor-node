# Monitor Node — 远端 Server 地址
SERVER_WS_URL=wss://<server-ip>:8443/ws
SERVER_RTMP_URL=rtmp://<server-ip>:1935/live

# 调试模式：开启后推流所有设备到本地 RTMP，供 OBS 拉流验证
STREAM_DEBUG=false

# 应用
SECRET_KEY=change-me-to-a-random-string
DEBUG_INFO=false
